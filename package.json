{
  "name": "unpissed-app",
  "version": "0.6.1",
  "private": true,
  "description": "Supabase-backed mobile-first PWA for Unpissed with Leaflet map, geolocation and fixed mobile navigation.",
  "scripts": {
    "dev": "npx serve .",
    "geo:seeds": "node tools/geocode_seed_locations.mjs",
    "check": "node --check js/app.js && node --check js/config.js && node --check js/config.example.js && node --check js/vendor-fallback.js && node --check js/supabase-api.js && node --check netlify/functions/health.js && node --check tools/geocode_seed_locations.mjs"
  },
  "keywords": [
    "pwa",
    "netlify",
    "supabase",
    "bathroom",
    "check-in"
  ],
  "license": "UNLICENSED"
}
